# Configuración para Smithery.ai
name: governagent
version: 1.0.0
description: "Proxy MCP con pagos automáticos HTTP 402 para optimización SQL y auditoría de seguridad premium."

author: NoHumanHands
repository: https://github.com/NoHumanHands/governagent

commands:
  start:
    command: node
    args:
      - dist/index.js
    env:
      PORT: 3000

pricing:
  model: pay-per-use
  currency: USD
  tools:
    optimize-sql: 0.01
    security-audit: 0.50
    audit-logs: 0.05
    generate-report: 0.25

# Configuración del servidor remoto (Render)
deployment:
  type: remote
  url: https://governagent.onrender.com/sse
